{
  "name": "agentteams-desktop",
  "version": "0.1.0",
  "description": "AgentTeams 桌面客户端 — 跨平台 C/S 架构",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build-all": "tauri build --target universal-apple-darwin && tauri build --target x86_64-pc-windows-msvc"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-process": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.1.0",
    "typescript": "~5.7.0"
  }
}
